# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -XX:MaxMetaspaceSize=512m
kotlin.daemon.jvm.options=-Xmx2048m -XX:MaxMetaspaceSize=512m

# Enable parallel execution and configuration cache for better performance
org.gradle.parallel=true
org.gradle.configuration-cache=true

# Kotlin settings
kotlin.code.style=official
android.disallowKotlinSourceSets=false

# Use toolchains instead of hardcoded paths for better portability
# org.gradle.java.home=C:/Users/snehi/.jdks/corretto-21.0.10
